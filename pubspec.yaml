name: super_tooltip
description: Super flexible Tooltip class that gets opened in the screens overlay
version: 0.9.5
homepage: https://github.com/escamoteur/super_tooltip

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints:
  flutter_test:
    sdk: flutter
